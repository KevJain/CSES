#include <algorithm>
#include <iostream>
#include <vector>
#include <string>
typedef long long ll;
using namespace std;

int main() {
    int n, k;
    cin >> n >> k;
    // Choose the movie with the earliest finish time that we can watch
    return 0;
}
