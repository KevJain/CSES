#include <iostream>
#include <vector>
#include <string>
typedef long long ll;
using namespace std;

int main() {
    int n, m;
    cin >> n >> m;
    vector<int> arr(n);
    for (auto& a : arr) {
        cin >> a;
    }
    int mod = 1e9 + 7;
    ll out = 0;

    return 0;
}
