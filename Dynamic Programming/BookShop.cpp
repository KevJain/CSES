#include <iostream>
#include <vector>
#include <string>
typedef long long ll;
using namespace std;

int main() {
    int n, x;
    cin >> n >> x;
    vector<int> price(n);
    vector<int> pages(n);
    for (auto &p : price) {
        cin >> p;
    }
    for (auto &p : pages) {
        cin >> p;
    }
    vector<int> max_pages(target + 1);
    return 0;
}
